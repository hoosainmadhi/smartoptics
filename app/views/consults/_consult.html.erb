
<ol>
<%= "#{consult.service_date}"%> 
<%= "#{consult.reservation_number}"%>
<%= link_to 'Edit', edit_patient_consult_path(consult.patient_id,consult.id) %> | 
<%= link_to 'Delete', patient_consult_path(consult.patient_id,consult.id), method: :delete, data: { confirm: 'Are you sure?' } %>

</ol>




